# 🎵 Telegram Music Mini App - Setup Guide

Полное руководство по настройке и запуску Telegram Mini App для поиска и прослушивания музыки.

## 📋 Требования

### Backend
- Python 3.7+
- pip

### Frontend
- Node.js 16+
- npm или yarn

## 🚀 Быстрый старт

### 1. Установка зависимостей

#### Backend
```bash
cd backend
pip install -r requirements.txt
```

#### Frontend
```bash
npm install
```

### 2. Настройка окружения

#### Backend
Создайте файл `.env` в папке `backend`:
```bash
cd backend
cp .env.example .env
```

#### Frontend
Создайте файл `.env.local` в корне проекта:
```bash
cp .env.example .env.local
```

Отредактируйте `.env.local`:
```env
VITE_API_URL=http://localhost:8000
```

### 3. Запуск приложения

#### Запуск Backend
```bash
cd backend
python main.py
```

Backend будет доступен по адресу: `http://localhost:8000`
API документация: `http://localhost:8000/docs`

#### Запуск Frontend
```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173`

## 🤖 Настройка Telegram Bot

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный **Bot Token**

### 2. Настройка Web App

1. Отправьте команду `/mybots` в BotFather
2. Выберите вашего бота
3. Выберите **Bot Settings** → **Menu Button**
4. Выберите **Configure menu button**
5. Введите название кнопки (например, "🎵 Музыка")
6. Введите URL вашего приложения

#### Для локального тестирования

Используйте **ngrok** для создания HTTPS туннеля:

```bash
# Установка ngrok (Windows)
choco install ngrok

# Запуск туннеля для frontend
ngrok http 5173
```

Скопируйте HTTPS URL из ngrok (например, `https://abc123.ngrok.io`) и используйте его в BotFather.

**Важно:** Также запустите ngrok для backend:
```bash
ngrok http 8000
```

И обновите `VITE_API_URL` в `.env.local` на HTTPS URL backend.

#### Для production

Разверните приложение на хостинге с HTTPS (например, Vercel, Netlify для frontend и Railway, Render для backend).

### 3. Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите кнопку Menu (🎵 Музыка)
3. Mini App должно открыться

## 🔧 Разработка

### Структура проекта

```
tg-music-player/
├── backend/
│   ├── hitmo_parser.py    # Парсер Hitmo
│   ├── main.py            # FastAPI приложение
│   ├── requirements.txt   # Python зависимости
│   └── .env.example       # Пример конфигурации
├── components/            # React компоненты
├── views/                 # Страницы приложения
├── utils/
│   ├── api.ts            # API клиент
│   └── telegram.ts       # Telegram WebApp утилиты
├── context/              # React контексты
├── App.tsx               # Главный компонент
└── index.html            # HTML шаблон
```

### API Endpoints

#### `GET /api/search`
Поиск треков

**Параметры:**
- `q` (string, required) - Поисковый запрос
- `limit` (int, optional) - Максимальное количество результатов (по умолчанию: 20)

**Пример:**
```bash
curl "http://localhost:8000/api/search?q=Imagine%20Dragons&limit=10"
```

#### `GET /health`
Проверка работоспособности API

**Пример:**
```bash
curl "http://localhost:8000/health"
```

### Telegram WebApp API

Приложение использует [Telegram WebApp API](https://core.telegram.org/bots/webapps) для интеграции с Telegram:

- Инициализация: `initTelegramWebApp()`
- Получение данных пользователя: `getTelegramUser()`
- Тактильная обратная связь: `hapticFeedback.light()`, `hapticFeedback.success()`, и т.д.
- Управление темой: `getColorScheme()`, `getThemeParams()`

## 🌐 Деплой

### Frontend (Vercel)

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Настройте переменные окружения:
   - `VITE_API_URL` - URL вашего backend API
4. Deploy!

### Backend (Railway)

1. Создайте аккаунт на [Railway](https://railway.app)
2. Создайте новый проект из GitHub
3. Выберите папку `backend` как root directory
4. Railway автоматически определит Python и установит зависимости
5. Приложение будет доступно по HTTPS URL

### Backend (Render)

1. Создайте аккаунт на [Render](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройте:
   - **Root Directory**: `backend`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
5. Deploy!

## 🐛 Устранение неполадок

### Backend не запускается

**Проблема:** `ModuleNotFoundError: No module named 'fastapi'`

**Решение:**
```bash
cd backend
pip install -r requirements.txt
```

### Frontend не может подключиться к API

**Проблема:** `Failed to fetch` или CORS ошибки

**Решение:**
1. Убедитесь, что backend запущен
2. Проверьте `VITE_API_URL` в `.env.local`
3. Проверьте CORS настройки в `backend/main.py`

### Telegram WebApp не открывается

**Проблема:** Кнопка Menu не работает

**Решение:**
1. Убедитесь, что URL использует HTTPS (для production или ngrok для локального тестирования)
2. Проверьте настройки Menu Button в BotFather
3. Попробуйте переоткрыть бота

### Поиск не работает

**Проблема:** Результаты поиска не отображаются

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что backend доступен
3. Проверьте, что сайт rus.hitmotop.com доступен
4. Попробуйте другой поисковый запрос

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в GitHub репозитории.
